/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'https://cliqueproject-production.up.railway.app/',
    ],
  },
};

export default nextConfig;